<%
    var title = '';
    if (page.title) {
        title = __(page.title);
    }
    
    var postList = [];
    site.tags.forEach((i) =>{
        if (i.name == page.tag)
            postList = i.posts;
    } );
%>
<!-- Banner -->
<%- partial('_partial/banner.ejs', {
    mainText: title || theme.default_post_title || __('default_post_title'),
    secondaryText: '',
    faIconCss: theme.tag_font_awesome_icon_css
}) %>